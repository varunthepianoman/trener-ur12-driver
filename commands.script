def move_home():
    movej([0, -1.5705, 1.5707, 0, 0, 0])
end
def move_first_joint(joint_val=0):
    curr_joint = get_actual_joint_positions()
    curr_joint[0] = curr_joint[0] + joint_val
    movej(curr_joint)
end
